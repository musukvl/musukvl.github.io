@using Amba.NotFoundLog.Services
@{
    WorkContext.Resolve<INotFoundLogService>().LogNotFound((string)Model.RequestedUrl, (string)Model.ReferrerUrl);
}
<h1>@Html.TitleForPage(T("Not found").ToString())</h1>
<p>@T("The page you are looking for does not exist.")</p>

